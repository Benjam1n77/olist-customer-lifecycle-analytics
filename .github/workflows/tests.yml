name: Python tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Verify pipeline registry
        run: python run_pipeline.py --list-stages

      - name: Run unit tests
        run: pytest -q

      - name: Execute public showcase notebook
        run: >
          jupyter nbconvert --to notebook --execute notebooks/01_project_showcase.ipynb
          --output-dir /tmp/olist-notebook-check --ExecutePreprocessor.timeout=180
